- remove toUpperCase before state (expected) [compare case insensitive].
- verified
    => in data
    => requires ui renders
    => requires listen to data

- Put WebComp in src to avoid TS strange behavior when type checking.